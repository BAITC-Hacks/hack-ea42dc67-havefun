pandas>=2.0
numpy>=1.24
fastapi>=0.115
uvicorn>=0.30
# Нужен только для объяснений на естественном языке. Без ключа не вызывается,
# но пакет должен быть установлен, иначе объяснение молча уходит в шаблон.
openai>=1.50
